function toggleDarkMode() {
  document.body.classList.toggle('dark-mode');
}

function baixarPDF() {
  alert("Para baixar o currículo, use Ctrl+P (ou Command+P no Mac) e selecione 'Salvar como PDF'.");
}
